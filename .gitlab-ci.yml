rsync publish:
  before_script:
    - source activate pel
    - pip install -r requirements.txt
  script:
    - which pelican
    - make publish
    # - PELICAN=`which pelican` make rsync_upload
  tags:
    - shell
